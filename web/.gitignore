# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

# npm is the package manager for `web/` (`package-lock.json` + `npm ci` in CI).
# Ignore alternate lockfiles so local pnpm/yarn runs do not create untracked churn.
pnpm-lock.yaml
yarn.lock

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Playwright
/test-results/
/playwright-report/
/blob-report/
